# Baseline Validation

CareHub Appointment Service — demo target for ShipSafe AI.
Validation performed prior to the analysis-engine implementation phase.

---

## Environment

| Item | Value |
|---|---|
| Python | 3.11.9 |
| pytest | 9.1.1 |
| pytest-cov | 7.1.0 |
| Flask | 3.1.3 |
| Platform | win32 |

---

## Test Results

**Command:**
```
pytest demo_target/tests/ -v --tb=short
```

**Result:** 38 passed, 0 failed, 0 errors

**Test breakdown by file:**

| File | Tests | Result |
|---|---|---|
| `tests/test_health.py` | 2 | passed |
| `tests/test_patients.py` | 7 | passed |
| `tests/test_doctors.py` | 7 | passed |
| `tests/test_appointments.py` | 16 | passed |
| `tests/test_notifications.py` | 6 | passed |
| **Total** | **38** | **passed** |

All tests use isolated temporary SQLite databases created per test session via `conftest.py`.
No test shares state with another.

**Requirements coverage:**

| Requirement | Covered by test | Test name(s) |
|---|---|---|
| R001 — No reminders for cancelled appointments | ✓ | `test_cancelled_appointment_does_not_trigger_reminder`, `test_cancelled_appointment_reminder_returns_error_message` |
| R002 — API response includes `appointment_id`, `status`, `eta_minutes` | ✓ | `test_create_appointment_response_includes_required_fields`, `test_cancel_appointment_response_includes_required_fields` |
| R003 — Priority persisted | ✓ | `test_appointment_priority_persisted_normal`, `test_appointment_priority_persisted_high`, `test_appointment_priority_persisted_emergency` |
| R004 — Parameterized queries | ✓ (verified by code review; all queries use `?` placeholders) | N/A — no runtime SQL injection test in baseline |
| R005 — Regression tests present and passing | ✓ | All 38 tests present and passing |

---

## Coverage

**Command:**
```
pytest demo_target/tests/ --cov=demo_target --cov-report=term-missing -q
```

**Total: 98% (434 statements, 10 missed)**

| File | Stmts | Miss | Cover | Missed lines |
|---|---|---|---|---|
| `app.py` | 27 | 2 | 93% | 54–55 |
| `config.py` | 5 | 5 | 0% | 3–9 |
| `db.py` | 20 | 0 | 100% | — |
| `models.py` | 8 | 0 | 100% | — |
| `routes/__init__.py` | 0 | 0 | 100% | — |
| `routes/appointments.py` | 42 | 0 | 100% | — |
| `routes/doctors.py` | 24 | 0 | 100% | — |
| `routes/patients.py` | 27 | 0 | 100% | — |
| `services/__init__.py` | 0 | 0 | 100% | — |
| `services/appointment_service.py` | 29 | 0 | 100% | — |
| `services/notification_service.py` | 16 | 3 | 81% | 47–53 |
| `tests/__init__.py` | 0 | 0 | 100% | — |
| `tests/conftest.py` | 25 | 0 | 100% | — |
| `tests/test_appointments.py` | 90 | 0 | 100% | — |
| `tests/test_doctors.py` | 37 | 0 | 100% | — |
| `tests/test_health.py` | 9 | 0 | 100% | — |
| `tests/test_notifications.py` | 42 | 0 | 100% | — |
| `tests/test_patients.py` | 33 | 0 | 100% | — |

**Notes on uncovered lines:**

- `app.py` lines 54–55: the `if __name__ == "__main__"` block (`application = create_app()` / `application.run(...)`). Not exercised by the test suite; this is expected for an entry-point guard.
- `config.py` lines 3–9: the standalone config module is not imported by the application factory (the factory uses direct `app.config` assignment). `config.py` is a documentation artifact; it is not dead code in a harmful sense, but it is unused at runtime.
- `notification_service.py` lines 47–53: the `get_notifications_for_appointment()` helper function. It is present for future use and is not currently called by any route or test. Not a defect.

---

## Application Validation

**Startup command:**
```
python demo_target/app.py
```

**Startup output observed:**
```
* Serving Flask app 'app'
* Debug mode: on
* Running on http://127.0.0.1:5050
```

Flask started successfully with the development server on port 5050.

**Health check:**

Request:
```
GET http://127.0.0.1:5050/health
```

Response:
```
HTTP 200 OK
Content-Type: application/json

{
  "service": "CareHub Appointment Service",
  "status": "ok"
}
```

Result: **PASS**

---

## Requirements Validation

### Markdown file

**Path:** `requirements/CareHub_v2_4_Requirements.md`

**Verified present:** yes

**Requirements confirmed in file:**

| ID | Present |
|---|---|
| R001 | ✓ |
| R002 | ✓ |
| R003 | ✓ |
| R004 | ✓ |
| R005 | ✓ |

All five requirements are present with full acceptance criteria text.

### PDF file

**Path:** `requirements/CareHub_v2_4_Requirements.pdf`

**Verified present:** yes

**PDF header:** `%PDF` (valid PDF format)

**File size:** 2119 bytes

**Generated by:** `generate_requirements_pdf.py` using fpdf2

The PDF contains the same five requirements (R001–R005) with the same acceptance criteria as the Markdown file.

---

## Repository Hygiene

### `.gitignore` coverage

The following generated artifact types are confirmed excluded from version control:

| Pattern | Source in `.gitignore` | Covers |
|---|---|---|
| `__pycache__/` | line 2 | All Python bytecode directories |
| `*.py[codz]` | line 3 | `.pyc`, `.pyo`, `.pyd`, `.pyz` files |
| `.pytest_cache/` | line 51 | pytest cache directory |
| `.coverage` / `.coverage.*` | lines 43–44 | Coverage data files |
| `coverage.xml` | line 47 | Coverage XML report |
| `htmlcov/` | line 40 | Coverage HTML report |
| `*.db` | added during validation | SQLite database files including `carehub.db` |

**Note:** The `.gitignore` originally matched `db.sqlite3` but not the `*.db` pattern used by this project (`carehub.db`). The `*.db` pattern was added to `.gitignore` during this validation to ensure the runtime database is excluded.

### Confirmed excluded

- `demo_target/__pycache__/` — excluded by `__pycache__/`
- `demo_target/routes/__pycache__/` — excluded by `__pycache__/`
- `demo_target/services/__pycache__/` — excluded by `__pycache__/`
- `demo_target/tests/__pycache__/` — excluded by `__pycache__/`
- `.pytest_cache/` — excluded by `.pytest_cache/`
- `demo_target/carehub.db` — excluded by `*.db` (added during validation)

### Temporary validation artifacts

- `requirements/test_check.pdf` — **not present** (cleaned up before this validation)

### `generate_requirements_pdf.py`

This file is a **useful reproducible project artifact**. It can regenerate `requirements/CareHub_v2_4_Requirements.pdf` deterministically from the requirement text using fpdf2. It should be retained in the repository root. It is a standalone script (not a test, not application code) and does not affect the demo target or ShipSafe engine.

---

## Baseline Status

All validation checks passed with measured results:

- **38 tests collected, 38 passed, 0 failed**
- **98% coverage** (434 statements, 10 missed; all uncovered lines are expected non-defects)
- **Flask application starts** and serves requests correctly
- **GET /health returns HTTP 200** with the expected JSON body
- **R001–R005 present** in both the Markdown and PDF requirements files
- **Repository hygiene** confirmed; generated artifacts are excluded from Git

**The CareHub Appointment Service baseline is ready for the next phase.**

The next phase is the ShipSafe analysis engine: building the deterministic five-agent analysis system that will inspect changes to this baseline application.
